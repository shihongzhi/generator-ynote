language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ynote' ]; then cd .. && eval "mv $currentfolder generator-ynote" && cd generator-ynote; fi

